[package]
name = "aurora-logic"
version = "0.1.0"
edition = "2021"
description = "Host-testable pure-logic core shared with the Aurora aarch64 kernel"
license = "MIT"
repository = "https://github.com/pavanchow/aurora"
